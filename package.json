{
  "name": "mawazo",
  "version": "0.1.0",
  "private": true,
  "description": "Mawazo — AI-powered WhatsApp bookkeeping for Ugandan SMEs",
  "workspaces": [
    "packages/*",
    "shared"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "build:ordered": "npm run build -w shared && npm run build -w packages/momo && npm run build -w packages/ai-engine && npm run build -w packages/webhook && npm run build -w packages/database",
    "dev": "npm run dev -w packages/webhook",
    "migrate": "npm run migrate -w packages/database",
    "lint": "npm run lint --workspaces --if-present",
    "smoke-test": "bash scripts/smoke-test.sh"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
