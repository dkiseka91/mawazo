[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build:ordered"

[deploy]
startCommand = "node packages/webhook/dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
